Neeche **simple wording + step-by-step** explain kar raha hoon yeh PHP array (translations.php) **hum ne kis tarah aur kis purpose ke liye use kiya hai**.

Yeh explanation aap documentation, sir ko explain karne, ya apne project report me use kar sakte ho.

---

# ✅ **Yeh File Kya Hai? (Easy Explanation)**

Yeh file **Laravel me language translation dictionary** ke taur par use hoti hai.

➡ **Key = Original English Text**
➡ **Value = Us ki Urdu (ur) aur French (fr) translation**

Is file ko hum `translations.php` ke naam se **storage/app/public** me rakhte hain.

Hum PDF generate karte waqt jab koi string translate karna hota hai to hum translation ko yahan se pick karte hain.

---

# 🧠 **Yeh File Kaise Kaam Kartī Hai? — Step by Step**

### **1️⃣ Har English text key hai**

Example:

```php
'Report Title' => [
    'ur' => 'رپورٹ کا عنوان',
    'fr' => 'Titre du rapport'
],
```

Iska matlab:

* Agar JSON me `"Report Title"` likha hua ho
* Aur language `"ur"` ho
  → To Urdu translation return hogi → `"رپورٹ کا عنوان"`

---

# 2️⃣ **Har Key ke andar multi-language translations hain**

Har key ke andar arrays hain:

| Language Code | Meaning            |
| ------------- | ------------------ |
| `ur`          | Urdu translation   |
| `fr`          | French translation |

---

# 3️⃣ **Laravel me Translation Function Isko Kaise Use Karti Hai?**

Aap ke Controller me yeh function hota hai:

```php
private function translate($text, $lang = 'ur')
{
    $translations = include(storage_path('app/public/translations.php'));
    return $translations[$text][$lang] ?? $text;
}
```

### ⭐ Is function ka simple logic:

1. `translations.php` load karta hai
2. Check karta hai:

   * kya yeh text **as a key** maujood hai?
   * kya is ke andar required language (`ur` / `fr`) exist karti hai?
3. Agar translation mil jaye → return translation
4. Agar na mile → original English text return karta hai

Example:

```php
$this->translate('Email', 'ur');
```

Return:

```
ای میل
```

---

# 4️⃣ **Yeh File JSON Ke Har Section Ko Translate Kartī Hai**

### → Header text translate hota hai

### → Hero section text translate hota hai

### → Footer text translate hota hai

### → Table title translate hota hai

### → Table headers translate hotay hain

### → Table rows bhi translate ho sakte hain (optional, agar translations add karen)

---

# 5️⃣ **Row Translation Ka Important Concept**

Aap ke table rows me values like:

```
"name": "Naida",
"email": "junaid@erp.com"
```

Yeh values **keywords** nahi hain is liye translation file me nahi hongi.
Sirf woh text translate hoga jo **explicitly English keys me diya gaya ho**, like:

* "Employee Name"
* "Phone"
* "Address"

---

# 🟦 **FINAL SUMMARY — Simple Words**

### ✔ Yeh file translation dictionary hai

### ✔ PDF banate waqt sari English lines ko Urdu ya French me convert karti hai

### ✔ Hum `translate()` function se key pick karte hain

### ✔ Agar available ho to Urdu/French return hoti hai

### ✔ Agar nahi ho to English text return hota hai

---

# 🟩 **If You Want — I Can Also Provide:**

✅ PDF Translation Full Flow Diagram
✅ Controller ka full clean optimized code
✅ Commented version of translation function
✅ Multi-language switch system
✅ Automatic translation catch (missing keys finder)
✅ JSON → PDF architecture documentation (sir ke liye)

Aap bata dein ❤️
